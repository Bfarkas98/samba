# samba szerver telepítése script-tel

Script indítása előtt:
sudo -s #(add meg a jelszavad)
apt-get update
apt-get upgrade -y
apt install ssh -y

#Script futtatása:
bash /home/#username/samba/samba-script
